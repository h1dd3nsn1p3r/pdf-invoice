interface Helpers {}

export const Helper: Helpers = {
	/**
	 * Render logo.
	 *
	 * @returns
	 * @since 1.0.0
	 */
	renderLogo() {},
	/**
	 * Calculate the total amount of the invoice.
	 *
	 * @returns {number} The total amount of the invoice.
	 * @since 1.0.0
	 */
};
